# Data Schemas

/public_taste/{uid} { vector, updatedAt }
/users/{uid}/buds/{friendUid} { since }
/users/{uid}/receipts/{rid} { subtotal, total }
/users/{uid}/receipts/{rid}/items/{iid} { name, qty, price }