import 'package:flutter/material.dart';

void main() => runApp(const _SoloDemoApp());

class _SoloDemoApp extends StatelessWidget {
  const _SoloDemoApp();
  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Diet/Allergy solo demo was archived. (stub)'),
        ),
      ),
    );
  }
}
