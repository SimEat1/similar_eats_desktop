import 'package:flutter/widgets.dart';

class MonetizationPlaceholder extends StatelessWidget {
  const MonetizationPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
