import 'package:flutter/widgets.dart';

class ReviewTaggingPlaceholder extends StatelessWidget {
  const ReviewTaggingPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
