import 'package:flutter/material.dart';

class ReviewTaggingScreen extends StatelessWidget {
  const ReviewTaggingScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Review Tagging')));
}
