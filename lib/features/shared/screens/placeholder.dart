import 'package:flutter/material.dart';

class SharedUtilitiesScreen extends StatelessWidget {
  const SharedUtilitiesScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Shared Utilities')));
}
