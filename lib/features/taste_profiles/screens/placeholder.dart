import 'package:flutter/material.dart';

class TasteProfilesScreen extends StatelessWidget {
  const TasteProfilesScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Taste Profiles')));
}
