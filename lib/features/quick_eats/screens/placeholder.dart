import 'package:flutter/material.dart';

class QuickEatsScreen extends StatelessWidget {
  const QuickEatsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Quick Eats')));
}
