import 'package:flutter/widgets.dart';

class QuickEatsPlaceholder extends StatelessWidget {
  const QuickEatsPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
