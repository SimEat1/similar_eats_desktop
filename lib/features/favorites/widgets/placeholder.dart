import 'package:flutter/widgets.dart';

class Favorites&TryListPlaceholder extends StatelessWidget {
  const Favorites&TryListPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
