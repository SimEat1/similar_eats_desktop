import 'package:flutter/widgets.dart';

class OfflineDriftDBPlaceholder extends StatelessWidget {
  const OfflineDriftDBPlaceholder({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
