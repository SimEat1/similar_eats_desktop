import 'package:flutter/material.dart';

class OfflineDriftDBScreen extends StatelessWidget {
  const OfflineDriftDBScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Offline Drift DB')));
}
